/** 
 * Header file for the signature grader
 * A submission to this problem automatically has this header prepended to the source.
 * It can then with the C grader (handler.c) via is_valid.
 **/
#ifndef _GRADER_HEADER_INCLUDED
#define _GRADER_HEADER_INCLUDED
#include <stdbool.h>
bool is_valid(int);
#endif
